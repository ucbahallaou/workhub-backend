# workhub-backend
building a multi-user task/worklist backend using FastAPI + PostgreSQL.  it’s a natural evolution of TaskFlow: instead of orchestrating internal tasks, we manage user-created tasks in a collaborative system — accounts, roles, permissions, CRUD operations, background jobs, caching, and full testing.
